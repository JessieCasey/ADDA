spring.datasource.url=jdbc:postgresql://localhost:5432/soft
spring.datasource.username=postgres
spring.datasource.password=421970

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgresPlusDialect

# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=3000KB
# Max Request Size
spring.servlet.multipart.max-request-size=3000KB
#Swagger
spring.mvc.pathmatch.matching-strategy=ant_path_matcher
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0

# You can specify your own secret key for JWT
jwt.secret=this is my custom Secret key for authentication
jwt.expiration=604800
jwt.header=Authorization

# "Put here your api key"
api.key=4bcf090f1603522d4218e2cea8b11111
