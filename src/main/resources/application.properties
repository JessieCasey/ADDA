spring.datasource.url=jdbc:postgresql://localhost/adda
spring.datasource.username=postgres
spring.datasource.password=421970
spring.jpa.generate-ddl=true
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update

# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=3000KB
# Max Request Size
spring.servlet.multipart.max-request-size=3000KB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.upload-dir=/Users/artem/Documents/JavaProjects/adda/uploads

#Swagger
spring.mvc.pathmatch.matching-strategy=ant_path_matcher
